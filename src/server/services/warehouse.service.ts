import { db } from '../db';\n\nexport const WarehouseService = {\n  async getWarehouses() {\n    // Récupération des entrepôts\n  },\n  \n  async getBoxes(warehouseId: string) {\n    // Récupération des boxes d'un entrepôt\n  },\n  \n  async reserveBox(boxId: string, userId: string, startDate: Date, endDate: Date) {\n    // Réservation d'une box\n  },\n  \n  async releaseBox(boxId: string) {\n    // Libération d'une box\n  }\n};