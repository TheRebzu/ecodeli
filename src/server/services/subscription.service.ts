import { db } from '../db';\nimport { PaymentService } from './payment.service';\n\nexport const SubscriptionService = {\n  async getUserSubscription(userId: string) {\n    // Récupération de l'abonnement de l'utilisateur\n  },\n  \n  async subscribe(userId: string, planId: string, paymentMethodId?: string) {\n    // Souscription à un plan\n  },\n  \n  async cancel(userId: string) {\n    // Annulation d'un abonnement\n  },\n  \n  async getAvailablePlans() {\n    // Récupération des plans disponibles\n    return [\n      {\n        id: 'free',\n        name: 'Free',\n        price: 0,\n        features: ['Accès de base']\n      },\n      {\n        id: 'starter',\n        name: 'Starter',\n        price: 9.90,\n        features: ['Assurance jusqu'à 115€/envoi', 'Réduction 5% sur l'envoi de colis']\n      },\n      {\n        id: 'premium',\n        name: 'Premium',\n        price: 19.99,\n        features: ['Assurance jusqu'à 3000€/envoi', 'Réduction 9% sur l'envoi de colis']\n      }\n    ];\n  },\n  \n  async applyDiscountForSubscriber(userId: string, amount: number, type: string) {\n    // Application d'une réduction selon le type d'abonnement\n  }\n};