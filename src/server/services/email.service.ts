import nodemailer from 'nodemailer';\n\n// Configuration du transporteur d'emails\nconst transporter = nodemailer.createTransport({\n  host: process.env.EMAIL_SERVER_HOST,\n  port: Number(process.env.EMAIL_SERVER_PORT),\n  auth: {\n    user: process.env.EMAIL_SERVER_USER,\n    pass: process.env.EMAIL_SERVER_PASSWORD,\n  },\n  secure: process.env.NODE_ENV === 'production',\n});\n\nexport const EmailService = {\n  async sendVerificationEmail(to: string, token: string) {\n    // Envoi d'un email de vérification\n  },\n  \n  async sendPasswordResetEmail(to: string, token: string) {\n    // Envoi d'un email de réinitialisation\n  },\n  \n  async sendWelcomeEmail(to: string, name: string) {\n    // Envoi d'un email de bienvenue\n  },\n  \n  async sendDeliveryConfirmation(to: string, deliveryId: string) {\n    // Envoi d'un email de confirmation de livraison\n  },\n  \n  async sendInvoice(to: string, invoiceId: string, pdfBuffer: Buffer) {\n    // Envoi d'une facture par email\n  },\n};