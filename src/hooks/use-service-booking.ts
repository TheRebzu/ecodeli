import { api } from './use-trpc';\n\nexport function useServiceBooking() {\n  const bookServiceMutation = api.service.book.useMutation();\n  \n  const bookService = (serviceId: string, date: Date) => {\n    return bookServiceMutation.mutateAsync({ serviceId, date });\n  };\n  \n  return {\n    bookService,\n    isBooking: bookServiceMutation.isLoading,\n    error: bookServiceMutation.error,\n  };\n}