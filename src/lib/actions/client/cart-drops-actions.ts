'use server';

export async function createCartDropAction(data: any) {
  // Action en cours de développement
  return { success: true };
}

export async function cancelCartDropAction(id: string) {
  // Action en cours de développement
  return { success: true };
}