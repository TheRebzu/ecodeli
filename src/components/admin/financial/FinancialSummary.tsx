'use client';

import React from 'react';

interface ComponentProps {
  // Définir les props ici
}

export function FinancialSummary({ /* props */ }: ComponentProps) {
  return (
    <div>
      {/* Contenu du composant */}
    </div>
  );
}

