{
    "buildCommand": "pnpm run prisma:generate && pnpm run build",
    "installCommand": "pnpm install",
    "framework": "nextjs",
    "functions": {
      "app/api/*/route.ts": {
        "maxDuration": 60
      }
    }
  }